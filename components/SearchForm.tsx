'use client';
import { useState } from 'react';
import { toast } from 'sonner';
export default function SearchForm(){ const [roll,setRoll]=useState(''); const [loading,setLoading]=useState(false); const [err,setErr]=useState(''); async function submit(e:React.FormEvent){e.preventDefault(); setErr(''); setLoading(true); const r=await fetch('/api/search',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({rollNumber:roll})}); const j=await r.json(); setLoading(false); if(!r.ok){setErr(j.error||'Certificate not found.'); toast.error('Certificate not found'); return} location.href=`/certificate/${encodeURIComponent(j.rollNumber)}` } return <form onSubmit={submit} className="glass space-y-4 rounded-3xl p-4 shadow-glow"><div className="flex flex-col gap-3 sm:flex-row"><input value={roll} onChange={e=>setRoll(e.target.value)} placeholder="Enter Roll Number" className="min-h-14 flex-1 rounded-2xl border bg-white px-5 outline-none focus:ring-4 focus:ring-blue-100"/><button disabled={loading} className="rounded-2xl bg-blue-700 px-8 py-4 font-semibold text-white shadow-lg disabled:opacity-60">{loading?'Searching...':'Get Certificate'}</button></div><p className="text-sm text-slate-500">Certificates are available only for eligible participants.</p>{err&&<div className="rounded-2xl border border-red-200 bg-red-50 p-4 text-red-700">Certificate not found.<br/>Please verify your roll number or contact the organizers.</div>}</form>}
